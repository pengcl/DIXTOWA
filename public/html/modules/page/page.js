'use strict';

app.directive('ngPage', ['$location', 'appSvc', function ($location, appSvc) {
    return {
        restrict: 'A',
        scope: {},
        link: function (scope, element, attrs) {

        }
    };
}]);
